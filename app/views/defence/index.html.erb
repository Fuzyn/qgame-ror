<% if flash[:notice] %>
  <div class="success-messages">
    <p><%= flash[:notice] %></p>
  </div>
<% elsif flash[:alert] %>
  <div class="error-messages">
    <p><%= flash[:alert] %></p>
  </div>
<% end %>

<% @defence_queue.each do |element| %>
  <%= render partial: "common/queue", locals: { element: element, path: defence_path } %>
<% end %>

<% @planet_defence.each do |element| %>
  <%= render partial: "defence/cannon", locals: { element: element } %>
<% end %>
