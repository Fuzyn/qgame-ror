<div class="bonus_value building">
  <span class="title"><%= title %>:</span>
  <span class="<%= value && value > 0 ? 'text-green' : value && value < 0 ? 'text-red' : '' %>"><%= value %></span>
</div>